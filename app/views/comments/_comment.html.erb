
<% @blog_entry.comments.each do |comment| %>
  <div class="content">
    <%=comment.content %>
  <div>last update: <%=comment.updated_at %></div>
  </div>
  <div class="information">create at: <%=comment.created_at %> by <%= link_to "#{comment.user.email if comment.user}", comment.user %></div>
<%end%>


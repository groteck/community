<% @blog_entry.comments.each do |comment| %>
<% if comment.user %>
  <%='<h1>Comments:</h1>' %>
  <div class="content">
    <%=comment.content %>
  <div><%= 'last update: '%>  <%= comment.updated_at%></div>
  </div>
  <div class="information"><%='create on: '%> <%= comment.created_at %> <%' by '%>  <%= link_to "#{comment.user.email}", comment.user %></div>
<div><%= link_to 'Edit', edit_blog_entry_comment_path(@blog_entry, comment) %>
 <%= link_to 'Destroy', blog_entry_comment_path(@blog_entry, comment), confirm: 'Are you sure?', method: :delete %> </div>
<%end%>
<%end%> 

